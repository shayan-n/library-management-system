#include "Libraries/account.h"

int main() {
    User newUser;
    newUser.username = "leilabajelan";
    newUser.address = "noen";
    newUser.id = 10;
    newUser.lastname = "bajelan";
    newUser.name = "leila";
    newUser.phoneNumber = "09108406816";
    newUser.sex = "female";
    newUser.password = "123456";

    // char *username = (char *) malloc(1000);
    // char *password = (char *) malloc(1000);
    
    // printf("Enter your username: ");
    // scanf("%s", username);
    // printf("Enter your password: ");
    // scanf("%s", password);

    // // User user = logIn(username, password);

    // if (isAPassword(username)) printf("hell yeah\n");
    // else printf("no fuck\n");

    // free(username);
    // free(password);
}